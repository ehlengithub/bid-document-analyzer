interface:
  display_name: "招标文件拆解"
  short_description: "拆解招标文件并生成客户版清单和内部销售判断材料包模板"
  default_prompt: "Use $bid-document-analyzer to analyze this tender document and produce a client-ready checklist plus an internal bid/sales brief."
policy:
  allow_implicit_invocation: true
